doctype html
html
    head
        title Error
        link(rel="stylesheet", href="/styles.css")
    body
        main.content
            h1 Error

            section.container
                if mensaje
                    p #{mensaje}
                else
                    p Ha ocurrido un error inesperado.
                
                a(href="/login") Volver a intentar